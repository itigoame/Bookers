<h2><strong>New Book(不使用）</strong></h2>

<%= form_with model: @booker do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Body</h4>
  <%= f.text_area :body %>

  <%= f.submit'投稿' %>

<% end %>